const Discord = require("discord.js");
exports.run = (client, message, args) => {
    message.channel.send("Discord recently removed `localStorage`, so the old method of getting your token no longer works, but fear not folks, open up your developer tools like normal and follow this easy image guide!\nhttp://i.imgur.com/cODKB5e.png")
};
